目前進行到GUIItem，正在努力實作。基礎道具已經有了按鈕、滑條、勾選欄、文字顯示盒，未來預計增加卡片、
Act實體(只進行Check/ChangeState，沒有draw)、文字輸入欄、窗口(可移動)、用按鈕組合小算盤等。

1.目前已有的道具已經有了基本功能，但是在實現顯示/Act功能上各有不一。
	:只要遊戲時沒問題即可，至於具體使用上，可能要將每個函數的參數順序整理並統一。
	最好在實作Situation前完成。

2.在實作滑條時，發現用int做運算取float有缺陷。
	:遊戲時檢查是否有影響到功能，若無則不管，若有則做+1/-1微調。
	情況嚴重時，去網路上尋找轉型解決方法。(google: int to float c++)

3.GUIDrawingFunc的顏色選擇，導致了drawRect等函數需要實作多載，浪費空間。
	:可能之後將兩種多載整合至一起，並透過多加一個enum為無顏色來檢查是否使用SDL_Color參數。

4.幾個基本道具都有textWhere參數，用以簡單決定Name的顯示位置，但是麻煩，且成效如何尚且不知
	:實作Situation時看是否好用，若不好用則全部刪掉，之後改為TextBox實現。但Name可以保留。

5.尚未測試SDL_BlitSurface時傳入{0,0,0,0}做參數是否有問題？是否拖累效能?是否導致sdl_error?
	:測試越早越好。
6.實作TextBox時，需要有自己的surface，於是衍伸出問題，gEnd碰不到的Surface該如何free
	:每個物件都管理自己的surface，在解構時順便free，需要小心
	 (考慮)gEnd設立一個Surface**列表，所有物件在createSurface時都要註冊到這個列表上，
	 以免有未free的漏網之魚。
	 將兩個方法合起來，就很好。

