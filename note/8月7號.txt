在寫Text.h時出了問題
執行解構函式時，TTF_Close(Font)會導致存取違規
猜測是程式本身在執行TTF_Close之前就已經先幫我釋放掉記憶體了(像java那樣的垃圾管理)
實際問題有待查明。(不過應該不會對性能造成多大影響)

陷入兩難，是否應該重寫GUIItem，啟用GUIItemBase
許多Item沒有設置自己專屬的Texture，對未來圖形化有巨大影響
TextBox無法設定字體大小，有影響
新增了Image

Lua 需要學習 不只是為了以後可能換到love2d GameEngine，也為了GameCore